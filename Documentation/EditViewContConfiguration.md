# EditViewContConfiguration

``` swift
public struct EditViewContConfiguration<A, Cell:​ UITableViewCell>
```

## Initializers

### `init(initialValue:​configure:​)`

``` swift
public init(initialValue:​ [A], configure:​ @escaping (A, Cell) -> Cell)
```
